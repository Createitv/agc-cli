# Feature Docs

Each AppGallery Connect API family should receive a dedicated document in this directory before its endpoint adapters are marked production-ready.

Every feature document must include:

- CLI commands
- REST endpoints
- JSON examples
- Huawei API references
- Auth requirements
- Unsupported or browser-only boundaries
- Test coverage notes

